<p id="notice"><%= notice %></p>

<p class="blog title">
  <%= @artwork.title %>
</p>

<div class="blog">
  <%= image_tag url_for(@artwork.cover_image) if @artwork.cover_image.attached? %>
</div>

<div class="blog">
  <%= @artwork.description %>
</div>

<%= render @artwork.artwork_parts %>

<h3>Add a artwork part:</h3>
<%= render 'artwork_parts/form' %>

<%= link_to 'Back', root_path %>
